import { button } from "@/schemaTypes/definitions/button";
import { customUrl } from "@/schemaTypes/definitions/custom-url";
import { discoverCard } from "@/schemaTypes/definitions/discover-card";
import { featureCard } from "@/schemaTypes/definitions/feature-card";
import { pageBuilder } from "@/schemaTypes/definitions/pagebuilder";
import { richText } from "@/schemaTypes/definitions/rich-text";
import { statItem } from "@/schemaTypes/definitions/stat-item";

export const definitions = [
  customUrl,
  richText,
  button,
  pageBuilder,
  featureCard,
  discoverCard,
  statItem,
];
